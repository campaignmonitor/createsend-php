language: php

matrix:
  include:
    - php: 5.3
      dist: precise
    - php: 5.4
      dist: trusty
    - php: 5.5
      dist: trusty
    - php: 5.6
      dist: trusty
    - php: 7.0
      dist: trusty
    - php: hhvm
      dist: trusty
      env:
        - HHVMPHP7=1

before_script: composer install --dev
script: cd tests && php all_tests.php && cd ..
